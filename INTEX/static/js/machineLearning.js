function verifyForm() {
    if (confirm("Do you want to submit the recommender?")) {
        document.getElementById("formNo3").submit()
    }
}

function verifyPredictor() {
    if (confirm("Do you want to submit the predictor?")) {
        document.getElementById("formNo4").submit()
    }
}